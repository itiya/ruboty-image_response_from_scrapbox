RSpec.describe Ruboty::ImageResponseFromScrapbox do
  it "has a version number" do
    expect(Ruboty::ImageResponseFromScrapbox::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
